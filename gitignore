# AI 3D Studio - Git Ignore Rules
# These files are NOT uploaded to GitHub

# Live session data (changes every run)
state.json

# Generated 3D models (too large for GitHub)
models/cache/
models/cache/*

# Server logs (not needed in repo)
logs/
logs/*
*.log

# Python compiled files
__pycache__/
*.py[cod]
*$py.class
*.pyo

# Environment and secrets (use Railway env vars instead)
.env
.env.local
.env.production

# Debug artifacts (auto-generated during development)
shapee_installed.flag
_last_gemini_script.py
_temp_*.py
_temp_*.glb
_export_*.obj
_export_*.fbx
_diag_pid.tmp

# Windows junk
Thumbs.db
Desktop.ini
*.lnk

# macOS junk
.DS_Store

# Test files
test_*.glb
test.glb

# Large zip archives
*.zip

# Shap-E model cache (very large)
shap_e_model_cache/

# DO NOT IGNORE THESE (must be in repo):
# server.py
# static/index.html
# models/presets/
# models/scripts/
# requirements.txt
# Dockerfile
# railway.json
# settings.json
# folders.json
# history.json
